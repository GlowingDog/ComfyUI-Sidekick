# ComfyUI-Sidekick — live task list

Full approved plan: `C:\Users\PC\.claude\plans\i-would-like-to-compressed-finch.md`. Architecture + rules: [CLAUDE.md](CLAUDE.md).

## P1 — Skeleton + live edits + Claude CLI
- [x] Repo scaffold, CLAUDE.md / PLAN.md, inner `sidekick/` package
- [x] config (masked secrets), sessions (items + sequenced events), bridge (browser RPC), pending (questions/permissions)
- [x] Tool registry + dispatch (validation, permission modes, tool cards, truncation)
- [x] Node catalog over `/object_info` (search, describe with COMBO truncation, combo option search)
- [x] Tool defs: get_workflow, get_node, search_node_types, get_node_type, get_combo_options, add_node, connect_nodes, disconnect, set_widget_values, update_node, remove_nodes, create/update/remove_group, edit_graph, ask_user
- [x] MCP endpoint (hand-written JSON-RPC over HTTP, per-turn bearer token)
- [x] Spike: `claude -p` ↔ HTTP MCP, `--setting-sources project` keeps OAuth and drops user hooks; fixtures recorded
- [x] Claude CLI provider (argv builder, stream-json mapper, resume, retry without resume)
- [x] Frontend: graphCtx, read/edit tools, connect auto-match, widget coercion, RPC executor, chat panel (Shadow DOM), sidebar tab, settings + chat list overlays
- [x] Unit tests: `tests/py/test_core.py` (12), `tests/js/pure.test.mjs` (5)
- [x] Live verification (2026-09-18, second instance on :8189): routes load, sidebar panel mounts, dev `call_tool` built a 7-node txt2img graph via one `edit_graph`, groups, combo/clamp coercion, connect diagnostics, one undo step per call, MCP 401 without token, real Claude CLI turn (haiku) changed cfg/sampler and added a linked PreviewImage
- [x] Known rough edge: `create_group` around nodes that sit inside another group's area overlaps it → solved by `auto_layout new_groups` (P3); the prompt steers the model there
- [ ] Note: node ids are **strings** in frontend 1.48.6 — always compare with `String()`; geometry setters on `LGraphGroup` throw unless the group is already in a graph
- [x] Fix (2026-09-18, user report "nowhere to paste a key"): `h()` flattened children one level only, so arrays passed through `openOverlay(...kids)` rendered as `[object HTMLDivElement]` text — provider cards and the chat list were unusable. Now `flat(Infinity)`. Verified live on :8188: cards show Base URL / API key / Fetch models / remove; dummy key on a junk entry saved → field cleared, hint shown, focus kept, raw key never returned; then cleared again.
- [x] Panel keeps `keydown/keyup/keypress/paste/copy/cut` inside the Shadow root (retargeting makes them look like they come from a `<div>`, so ComfyUI/LiteGraph shortcuts could fire while typing or pasting a key). Verified: nothing reaches `document`, node count unchanged.
- [ ] Pane tests (`web/dev/pane_tests.js`, `window.SidekickTests`) — must cover the settings + chats overlays (that bug shipped because only `node --check` had seen them)

## P2 — Other brains
- [x] OpenAI-compatible loop (`agent/loop_openai.py`: aiohttp SSE, tool-call delta accumulation, `reasoning_content`/`reasoning`, `stream_options` fallback on 400, history trimming, 40-step cap), `GET /sidekick/providers/models`, provider settings UI (base URL, write-only key, model datalist, add/remove)
  - Unit-tested against a local mock SSE server (`tests/py/test_openai_loop.py`) and since **proven against a real provider by the user's own sessions** (see "Driven by real use").
- [x] Codex provider (`agent/cli_codex.py`). Spike-verified flags (codex-cli 0.155.0): HTTP MCP via `-c mcp_servers.sidekick.url` + `bearer_token_env_var`; **`default_tools_approval_mode="approve"` fixes "approval policy is never"**; `developer_instructions`, `web_search="live"`, `tools.web_search` are valid keys; `exec <opts> resume <thread_id> -` works; `--ignore-user-config` keeps the login.
  - Mapper + argv unit-tested from fixtures (`tests/py/test_codex.py`). **Live turn through the real stack (ComfyUI + browser bridge) not yet run** — do this first next session.
  - Trick: `--strict-config` + `-m bogus-model` validates a config key without spending tokens (wrong key → instant error, right key → model 400).
- [ ] Codex loads `~/.agents/skills` (~58k input tokens/turn, mostly cached). No working off-switch found: `--disable skills`, `skills.enabled`, `skills.include_host` are unknown; `skip_host_skill_discovery` is under-development and had no effect.

## Driven by real use (2026-09-18) — done ahead of plan order
The user's first two real sessions (API brain, 123-node workflow; session files in `user/__sidekick/sessions/`) **proved the OpenAI-compatible loop against a real provider** (streaming, 46 tool calls in one turn, ask_user card) and exposed three problems:
- [x] `get_workflow` hit the 16k cap → the model made ~40 single `get_node` calls. Now: budgeted detail (`full` → `outline` → `index`, 40k budget, 48k cap via `Tool.max_chars`), filters `group` / `query` / `node_ids`, `get_node node_ids` (≤25, bad ids tolerated), new `trace_connections`. Verified live: 260 nodes → outline 20.6k chars, index 3.9k; group filter 13 nodes 4k with widgets.
- [x] "open a new workflow tab" was refused. Now: `list_commands` / `run_command` (121 commands; per-call risk via `Tool.risk_fn` + regex allowlist in `tooldefs/ui.py`; "allow for this chat" is per command id, never the whole tool) and `workflow_tabs` (list/new/switch/list_saved/open/save/close). Tab switch replicates `workflowService.openWorkflow` (`load()` + `app.loadGraphData(state, true, true, wf)`); verified state survives a round trip. These two are **not** wrapped in `withUndo` (a tab switch mid-call would write the new tab's graph into the old tab's history).
- [x] Nodes overflowed a group; model did coordinate math by hand. Now: `add_node group_id` (first free spot, group grows), `arrange_nodes` (row/column/grid from real sizes, `fit_group_id`, reports collisions, skips pinned), `update_group fit_to_contents`. Pure math in `web/tools/layoutMath.js` (node-tested).
- [x] Bug found while testing: `remove_group remove_nodes` deleted the box but left the nodes (LiteGraph's `group._children` is stale right after programmatic moves). Now uses the same geometry test as `get_workflow` (`read.groupMembers`). Verified incl. undo/redo.
- [x] `tests/py/test_tooldefs.py`: Python frontend tool names == `web/tools/index.js` table, no union types in schemas, < 45 tools, command risk, per-command grants, read-only mode.
- [x] User report "I have no tool for tabs" after the tools shipped: the server had not been restarted (`/sidekick/status` still said 16 tools; 21 after restart). Prompt now tells the model its tools can change mid-chat so it does not parrot an earlier refusal from the history.
- [x] Stale server is now visible: `/sidekick/status` returns `restart_needed` (any `sidekick/**/*.py` newer than process start); the panel shows a yellow banner, refreshed on mount, on every send and on reconnect. Verified: banner logic live; flag + route import via `tests/py/test_routes.py` (stubs ComfyUI's `server` so import-time mistakes in `routes.py` are caught before a restart).
- [ ] Claude CLI: `--system-prompt-snapshot` defaults to `on`, so resumed chats keep the system prompt they started with (tools still refresh via MCP). Verify `off` with a spike before using it.
- [ ] Still unverified: `workflow_tabs` save/close/open/list_saved against real saved files (only list/new/switch ran live); new tools through a real LLM turn (needs ComfyUI restart — Python tool defs changed).

## Vision (user request 2026-09-18: "a way for it to visually see comfyui and the graph")
- [x] `screenshot` tool (`web/tools/vision.js`). `graph`: re-renders the real LiteGraph canvas at a size and framing we choose (node_ids / group / whole workflow, ≤1568px, zoom ≤1.0, low-quality threshold and debug overlay off), grabs it and restores everything in one synchronous task — the user's view never moves and it works even when the canvas is 0×0. `viewport`: the user's current view. `ui`: whole tab through `getDisplayMedia` (needs a click → `client.requestGesture` + a share bar in the panel; stream reused until the user stops sharing).
- [x] Pixels travel separately from text: tool result `{__images__, text, thumb}` → `registry.dispatch_full` → (ok, text, images). MCP brains get `image` content blocks; the OpenAI-compatible loop appends a `user` message with `image_url` parts after the batch of tool results (tool messages are text-only there). Only the newest 2 screenshots keep pixels; nothing with pixels is written to session files; a 360px `thumb` is stored on the tool card so the user sees what the model saw.
- [x] Text-only models (DeepSeek): provider setting `vision` auto/on/off. Auto: a 4xx while images are in history → strip pixels, retry, and remember `(base_url, model)` as blind only if the retry succeeds.
- [x] Consent: `Tool.confirm` + `confirm_note` → permission card ("Screenshots are sent to the AI provider…") even in read-only mode (looking is not editing); "allow for this chat" covers the rest of the chat. `ui` additionally needs the browser's own share prompt.
- [x] Verified: canvas capture live on :8188 (viewed the JPEG myself: nodes, titles, widget values, links all legible; canvas state restored bit-for-bit); **Claude CLI really sees MCP image blocks** (spike, haiku: read "KSampler, steps 8, cfg 1.0" off the real screenshot, $0.005); `tests/py/test_vision.py` (7 tests: split, bad payloads, consent incl. read-only, MCP block, OpenAI image message, blind-model fallback, history hygiene).
- [ ] Unverified: `ui` target end to end (needs a human click + browser prompt); a real vision-capable API model; **Codex with MCP image blocks**; Vue-nodes mode (canvas shots lack nodes there — the tool warns and points to `ui`).
- [ ] Known limit: multiline text boxes and other DOM widgets are not on the canvas, so they are blank in `graph`/`viewport` shots (values are in `get_workflow`; `ui` shows them).

## P3 — Sophisticated editing + interaction (2026-09-19)
- [x] `auto_layout` (`web/tools/dagLayout.js` pure + `autoLayout.js` live). Layered left-to-right layout: longest-path columns, then every node slides right to sit next to its nearest consumer (short links); cycle links ignored (iterative DFS); order + heights by barycentre sweeps, heights solved per column with isotonic regression (`placeColumn`: no overlaps, order kept, a crowd centres on what it wants); **sockets line up** (edges carry the socket's y offset from `getInputPos/getOutputPos`); disconnected pieces are shelf-packed in reading order of where they were. **Groups are blocks**: tidied inside first, placed as one item, box refitted (title width respected), nesting supported, empty boxes keep their size. `new_groups` creates titled boxes from explicit node lists as part of the layout (palette colours) — the robust way to group, boxes can never overlap; works as an `edit_graph` op with `$refs`. Scope: whole workflow / `group` / `node_ids`; a partial layout stays anchored, pushes everything right of / below the old footprint aside by the growth (untouched groups move as units), and grows boxes that hold some of the moved nodes. Pinned nodes, and groups holding one, never move (`left_in_place`); leftovers reported as `overlaps_nodes` / `overlaps_groups`. `dry_run`. One undo step.
  - Verified live on :8189: 12-node build + 5 new groups in ONE `edit_graph` call (looked at the picture: clean, level links, no overlaps); group-scoped tidy pushed 9 nodes / 4 groups aside by 741px with zero clashes; undo/redo restore everything in one step; pinned node + frozen group stay put, scoped call on a frozen group errors helpfully; **146-node real workflow (copy): 6 ms, 0 node clashes, 0 box overlaps**.
  - **Vue-nodes check done**: `node.pos = [x,y]` (what `setNodePos` does) goes through the setter that calls the layout store's `moveNode` — verified with Vue nodes on (flag flipped in memory only): all 14 DOM nodes' `translate()` followed `update_node` and a whole `auto_layout` exactly. In-place `node.pos[0] = x` would NOT sync: never do that.
  - Deviations from the plan: no TB direction (ComfyUI sockets are left/right; `arrange_nodes column` covers vertical stacks); reroutes are ordinary small nodes (a column is only as wide as its widest node, so they cost ~75px); `group_mode` dropped — groups are always kept together (ignoring them leaves boxes around the wrong nodes).
  - Known limits: long links pass behind nodes of the columns they skip (no dummy nodes); a deep graph becomes a long strip (24 columns = 22,000px on the 146-node test) — band wrapping would fix that; subgraph IO nodes are not moved.
- [x] `context_menu` (`web/tools/menus.js`, pure matching in `menuMatch.js`): node / group / canvas right-click menus incl. everything node packs add. Submenus are opened by entry callbacks calling `new LiteGraph.ContextMenu(...)`, so while a path is walked that constructor is swapped for a recorder (nothing is drawn; restored in `finally`). A click is emulated the way LiteGraph does it (menu callback, entry callback, declared `submenu`). `list` = read, `invoke` = edit and wrapped in one undo step. Needs `LGraphCanvas.active_canvas = app.canvas` (only real mouse events set it; LiteGraph's Mode/Colors/Shapes read it) and selects the node first like a real right-click. Async entries get 1.5 s, then "still running (dialog?)". Verified: Mode > Never (one undo step, undo reverts), Colors > red, Shapes listing, canvas Add Node > latent listing, pack entries visible (rgthree, Pixaroma, tinyterra, Remote IO), bad path lists the entries.
- [x] `settings` (`web/tools/settings.js`): search / get / set over `app.extensionManager.setting` (`settings` = definitions). `set` is `risky` (persists, no undo; grant is per exact id+value), coerces booleans / numbers (clamped to attrs) / option text or value, refuses `hidden` and custom-rendered settings, masks values whose id/name smells like a secret. Verified live read-only + a same-value set; **a real value change was not exercised** (would persist in the user's settings).
- [x] Run and watch (`web/tools/run.js`, report wording pure in `runReport.js`): `queue_prompt` goes through `app.queuePrompt` (seed widgets roll, errors light up, progress UI tracks it) and listens in on `api.queuePrompt` for the prompt id / rejection; waits by default. `wait_for_execution` polls `/history/<id>` + `/queue` (stateless: a 5 ms cached run cannot be missed), progress events only feed the "still running" line; without an id = the running job, else the last finished one. Verified live: validation rejection per node, runtime exception with traceback tail (SaveImage refusing `../../`), success with output files, batch of 2 without waiting, **ids inside subgraphs ("3:2") resolved to the inner node**.
- [x] `workflow_tabs list_templates / open_template` (497 core templates from `/templates/index.json` + node-pack examples via `api.getWorkflowTemplates()`), `load_workflow` (saved format or API format → always a NEW tab; API format is refused with the list of missing node types, else loaded via `app.loadApiJson` and auto-laid-out; tab names never collide with a saved file, because loading under a saved name binds the content to that file).
- [x] Subgraphs: outlines say where the canvas is (`INSIDE subgraph "X" = root node 3`), subgraph nodes read as `subgraph "Name" [N nodes inside]` instead of a UUID, `get_node` points at the way in, `subgraph` tool lists / enters (`node_id` or a root path like `57:12`) / exits / root. Navigation is `read` risk (a view change). Everything else already worked on `app.canvas.graph`.
- [ ] Not done: band wrapping for very deep graphs; `get_workflow` reading inside a subgraph without navigating; slot menus (right-click on a socket); answering LiteGraph text dialogs opened by menu entries (P5 `ui_type`).
- [x] Real LLM turn through the whole stack (Claude CLI, haiku, :8189, 29 tools): `workflow_tabs new` → ONE `edit_graph` (3 × add_node, connect, `auto_layout` op with `$ref` node_ids + `new_groups`) → `queue_prompt` → answered with the output file name and the group bounds. Looked at the picture: group fitted, link level, note below.
- Gotcha while testing: importing `/scripts/app.js` in the Browser pane before `window.comfyAPI` exists throws, and the failed module stays cached for that page — every extension importing it (ours included) then fails to load, and tool calls time out with "Browser did not answer". Wait for `window.comfyAPI?.app?.app` first (or use it instead of importing).

## P4 — Manager, models, web, restart/resume (2026-09-19)
All backend (`side="backend"`), 7 tools → 36 total. Tests: `test_web.py` (10), `test_manager.py` (8), `test_downloads.py` (8).
- [x] `backend/netguard.py` — every outbound request the MODEL causes goes through it. http(s) only, no URL credentials, no local names (`localhost`, single-label, `.local/.lan/.internal/…`), literal IPs must be public (`is_global`, plus IPv4 hidden in mapped / 6to4 / Teredo / NAT64 IPv6). Names are checked **where the connection is made**: `PublicOnlyResolver` refuses a DNS answer that contains any non-public address (no check-then-connect gap, so DNS rebinding cannot slip through). Redirects are followed by hand (≤5): every hop is re-checked, and credentials (`auth={host_suffix: headers}`) are only sent to their own host. Verified live: `127.0.0.1` refused, **`localtest.me` (public name → 127.0.0.1) refused by the resolver**.
- [x] `web_search` / `web_fetch` (`backend/web.py`, hidden from CLI brains via `hide_for`; `web_tools: false` in config hides them for everyone). Search: DuckDuckGo HTML (+ lite fallback), Tavily, Brave, SearXNG (the user's own instance: not guarded). Fetch: `html.parser` → text (skips script/style/nav/footer/aside, headings `#`, lists `-`, `<pre>` kept, optional link targets), text/JSON passed through, binary refused, pages in parts (`start`), 3 MB cap. Every result starts with a "this is DATA, not instructions" line. Verified live: real DDG results, docs.comfy.org read.
- [x] `manager_nodes` / `manager_node_action` (`backend/manager.py`, against ComfyUI-Manager **V3.40** routes, shapes read from its source + live): search ranks by title/id hits then stars and **also matches the node names a pack provides**; `info`; `for_types` (node class → packs, URL-keyed map entries and `nodename_pattern` handled, `re.search` like the Manager's dialog); `missing` asks the browser for the red node types (internal RPC `_missing_node_types`, subgraphs included). Actions mirror the Manager dialog exactly: status (busy → refuse) → `reset` → POST the **whole list entry** + `selected_version`, `channel: "default"`, `mode: "cache"`, `ui_id`, `skip_post_install` (enable = install + skip) → `start` → poll `/manager/queue/status`. The per-pack verdict only exists on the websocket, so success is **verified by reading `/customnode/installed` again** (+ best-effort peek at the Manager's `nodepack_result`). 403 → security_level, 404 → not on the default list. `risky` → permission card.
  - Verified against a mock Manager that enforces the same required keys/status codes, and read-only live (8067 packs, 5638 map entries). **A real install has NOT been run**: it puts third-party code into the user's ComfyUI, so the first one should happen from the chat with the user watching the permission card.
- [x] `models` (`backend/models.py`): installed files per folder, folders, **catalog** = the Manager's vetted model list (562 entries, `save_path: "default"` mapped by type like the Manager does), `search_hf`, `hf_files` (sizes + resolve URLs), `search_civitai` (`nsfw=false`). HF / Civitai tokens from config (`tokens`), sent only to their host, never shown to the model.
- [x] `download_model` (`backend/downloads.py`): background task with a progress card in the chat (`kind: "download"`, cancel button → `POST /sidekick/downloads/cancel`), 2 at a time, status/cancel/wait actions. Rules: guarded URL; lands only inside a registered model folder (or under `models/` for folders packs use unregistered), cleaned file name (server's `Content-Disposition` when none given), never overwrites, `.part` then rename, disk-space check, size check, sha256 computed (and verified when given), HTML answers refused (login pages), 401/403 → "needs a token". **Formats**: `.safetensors/.sft/.gguf` from anywhere; `.ckpt/.pt/.pth/.bin/.onnx` only when the URL is on the Manager's vetted list (the Manager's own rule at `security_level=normal`). Writes and hashing run in the executor so ComfyUI's event loop stays responsive. When done: node catalog cache dropped + browser asked to refresh combos (`_refresh_node_defs`).
- [x] `restart_comfyui` (`backend/restart.py`): the tool only **books** the restart (note + provider + model saved in `session.continuation`); `registry.dispatch` refuses further tool calls; the restart happens in `run_turn`'s `finally`, i.e. after the turn is over and saved, so the CLI's own session file is complete and resumable. Stop cancels the booking. Restart = the Manager's `/manager/reboot` (knows comfy-cli and the portable launcher), own `os.execv` fallback without it. Browser: on `reconnected` (or on page load) `client.resumeAfterRestart()` refreshes node definitions and calls `POST /sidekick/chat/resume`; the server hands the note out once (two tabs cannot resume twice), drops notes older than 30 min, and starts the next turn with the note wrapped in instructions (shown in the chat as "↻ ComfyUI restarted. Continuing: …").
- [x] Settings → "Web and downloads": web on/off, search engine + keys, SearXNG address, Hugging Face token, Civitai key (all write-only, with a forget button).
- [x] Verified live on :8189 (2026-09-19, dev harness + real Claude CLI turns): Manager search / for_types / **missing** (red nodes of a loaded workflow → pack `DZ-FaceDetailer`, through the browser RPC); models folders / installed / catalog / search_hf / hf_files / search_civitai; **a real download** (permission card → allow → 4.7 MB safetensors from Hugging Face → progress card `done`, sha256, listed by `models installed`; a gated repo gave the "needs a token" message; test file removed afterwards); web_search + web_fetch inside ComfyUI, `http://127.0.0.1:8189/sidekick/config` refused; **restart → auto-resume**: card → "Restart booked" → the model ended its turn → Manager reboot → browser reconnected → resumed turn answered with the note's content, `continuation` cleared.
- [x] **Bug found by that test, present since P1**: every streamed reply started with its first chunk doubled in the browser ("PINEAPPLEPINEAPPLE", and the user's earlier "II can't do that"). `item_add` events carried the live item dict; ComfyUI serializes websocket messages later from its publish loop, so the first delta (appended in the same tick) was already inside the `item_add` and then arrived again as a delta. The server's copy was always right, which is why session files and my earlier server-side checks looked fine. Fix: `add_item` emits a deep copy; regression test `EventTests` replays events the way the browser does.
- [ ] Not done / known limits: no resume of interrupted downloads (Range); downloads do not survive a ComfyUI restart (card turns "interrupted"); Civitai search is unauthenticated and SFW-only; page text extraction has no readability scoring (menus inside `<div>`s stay); CLI brains' native web tools still untested here; pack JS needs a page reload after install (the resume text tells the model to ask the user).

## P5 — Reach + polish
- [ ] Pop-out floating window, DOM automation (ui_snapshot/click/type/key), execute_js (gated), todos card, context trimming, README

## P6 — Power mode (opt-in)
- [ ] CLI file/shell tools with permission prompts bridged into the chat

## Deviations from the approved plan
- UI uses plain DOM + a tiny `h()` helper and a built-in safe markdown renderer instead of vendored Preact/htm/marked/DOMPurify: no downloads, no supply chain, matches the no-build convention.
- Python lives in one inner package `sidekick/` (not top-level `server/`, `mcp/`, `tools/`) to avoid shadowing ComfyUI's `server` module and the `mcp` pip package.
- Event catch-up is snapshot + seq (re-fetch on gap) instead of an `events?after=N` log.
